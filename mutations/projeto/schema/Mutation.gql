#import Usuario, Perfil, UsuarioInput from 'Usuario.graphql'

type Mutation {
    novoUsuario(dados: UsuarioInput!): Usuario!
    excluirUsuario(id: Int): Usuario
    alterarUsuario(id: Int, nome: String, email: String, idade: Int): Usuario
}